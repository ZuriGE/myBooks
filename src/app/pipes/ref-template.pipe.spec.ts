import { RefTemplatePipe } from './ref-template.pipe';

describe('RefTemplatePipe', () => {
  it('create an instance', () => {
    const pipe = new RefTemplatePipe();
    expect(pipe).toBeTruthy();
  });
});
